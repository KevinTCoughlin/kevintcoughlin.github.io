# NOTE: Production is served via GitHub Pages (see .github/workflows/deploy.yml).
# This file exists only so Netlify Deploy Previews keep working for PRs.
# Do NOT add security/cache headers here — they would not reflect production.

[build]
  publish = "."
  command = "echo 'Static site, no build needed'"

[build.environment]
  NODE_VERSION = "24"
  YARN_VERSION = "4.13.0"
