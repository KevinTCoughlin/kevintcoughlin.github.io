[build]
  # No build command needed - this is a static site
  command = ""
  publish = "."

[build.environment]
  # Explicitly set Node version for consistency
  NODE_VERSION = "20"

# Disable automatic framework detection
[build.processing]
  skip_processing = false
